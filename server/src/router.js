// this file is where specific routes are put into so that it can pass it to index.js

// const express = require('express');

// const router = express.Router();

// router.post('/login', require('./routes/loginRoute')) // this calls the content from the said path. 

// router.get('/todos', require('./routes/todosRoute'));

// module.exports = router;